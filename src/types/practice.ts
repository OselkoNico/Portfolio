import type { IconPractice } from "./icon";

export interface Practice {
        id: string;
        icon: IconPractice;
        title: string;
        description: string;
        finalNote: string;
}