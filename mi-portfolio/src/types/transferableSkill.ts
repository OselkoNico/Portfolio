import type { IconTransferableSkill } from "./icon";

export interface TransferableSkill {
        icon: IconTransferableSkill;
        title: string;
        description: string;
        finalNote: string;
}